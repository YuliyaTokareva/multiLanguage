import * as React from 'react';
import AuthorizationForm from '../multisite/components/authorizationForm/AuthorizationForm';

const Authorization: React.FC = () => {
  return <AuthorizationForm />;
};

export default Authorization;
